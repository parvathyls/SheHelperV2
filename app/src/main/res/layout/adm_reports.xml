<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="64dp"
    android:layout_centerHorizontal="true"
    android:layout_centerVertical="true"
    tools:context="com.example.android.ruraldevelopmenthackathonv1.AdmReports"
    >
    <Button
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Mothers close to due date"
        android:id="@+id/rd_duedtin30"
        android:onClick="Duedtin30days"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Infants (0-1 years)"
        android:id="@+id/rd_infants"
        android:onClick="Infants"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Toddlers (1-3 years)"
        android:id="@+id/rd_toddlers"
        android:onClick="Toddlers"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Swasthya Saathi Performance"
        android:id="@+id/rd_ss_perf"
        android:onClick="SSPerformance"
        />
</LinearLayout>